<div class="container">
<% movie = @movie%>
    <div>
      <img src="<%= movie.poster_url%>" alt="<%= movie.title %>" height= "300" width= "300">
    </div>
    <div>
      <h3><%= "#{movie.title}"%></h3>
      <p> <%= movie.overview %> </p>
      <h4> <%= movie.rating %> </h4>
    </div>
  </div>
